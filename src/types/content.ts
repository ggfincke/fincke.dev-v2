// Content types for static site content
export interface AboutContent
{
  heading: string;
  paragraphs: string[];
  email: string;
}

export interface HeroContent
{
  name: string;
  tagline: string;
}
